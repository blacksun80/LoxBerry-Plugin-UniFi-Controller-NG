# Keep line endings LF so shell scripts, configs and the plugin.cfg stay valid
# on the (Linux) LoxBerry even when edited/checked out on Windows.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
